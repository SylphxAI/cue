# Cue — video timeline evidence

The default `read_video` profile is `fast`: container metadata, streams, chapters, and embedded subtitles, with no scene detection, frames, or speech recognition.

```bash
npx @sylphx/cue
```

Tools: `read_video`, `video_evidence` (ops inside evidence tool). Not frame-by-frame VLM.
