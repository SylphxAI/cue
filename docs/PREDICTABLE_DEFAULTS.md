# Predictable defaults

`fast` probes streams and reads embedded subtitles. `quality` explicitly enables
scene detection, structural keyframes, OCR, or local ASR. No cloud video API or
frame-by-frame vision model is required.
